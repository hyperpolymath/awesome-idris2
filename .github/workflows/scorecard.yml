# SPDX-License-Identifier: PMPL-1.0-or-later
# This workflow is managed by gh actions-lock.
name: OSSF Scorecard

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scorecard:
    uses: hyperpolymath/standards/.github/workflows/scorecard-reusable.yml@28849f5da2a875129ba4cdfd46c6ddb1f750a9a7
    permissions:
      contents: read
      security-events: write
      id-token: write
